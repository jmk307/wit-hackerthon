spring:
  datasource:
    url: jdbc:mariadb://localhost:8000/exercise
    driver-class-name: org.mariadb.jdbc.Driver
    username: jimin
    password: simon1126
  jpa:
    hibernate:
      database-platform: org.hibernate.dialect.MariaDB103Dialect
      ddl-auto: update
      show-sql: 'true'
    properties:
      hibernate:
        format_sql: true
        ddl-auto: update
        show-sql: true
    show-sql: true

